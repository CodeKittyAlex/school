namespace ifelseif
{


    internal class programm
    {
        static void main(string[] args)
        {
            console.writeline("hello world");
            //peab saama sisestada nime
            //if ja else kontrollib, kas on nimi või tühik
            //kui leiate kuskilt teksti värviliseks teha, siis kasutage

            string nameof = console.readline();
            if (nameof = str){
                console.writeline("sisestatud nimi" +nameof):
                LabelTest.Text = nameof;
                LabelTest.ForeColor = System.Drawing.Color.Blue;
            }else{
                conseole.writeline("ei sisestenud nime");
            }
        }

    }
}